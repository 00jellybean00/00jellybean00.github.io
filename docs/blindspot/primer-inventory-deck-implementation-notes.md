# primer-inventory 매니저 브리핑 발표자료 구현 노트

- 시작일: 2026-07-27
- 연관 문서: [요구사항](2026-07-27-primer-inventory-deck-requirements.md) / [unknowns](2026-07-27-primer-inventory-deck-unknowns.md) / [설계 문서](2026-07-27-primer-inventory-deck-explainer.md)

<!-- 결정 시점마다 아래 형식으로 append. 사후 재구성 금지. -->

## 2026-07-27 — 구현 시작

- 결정: 설계 문서의 24장 뼈대와 부록 4장을 그대로 따라 `slides/primer-inventory/index.html` 한 파일로 만든다.
- 이유: 설계 문서가 검증 6회를 거쳐 장별 내용·줄 범위·글꼴 크기까지 확정했다. 구현 단계에서 다시 설계하지 않는다.
- 검토한 대안: 없음. 설계 단계에서 이미 대안을 다 검토했다.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-07-27 — 부록 코드를 소스 확인 없이 작성한 것을 발견하고 정정

- 결정: 부록 3에 넣은 `read_window` 코드를 실제 소스에서 다시 읽어 통째로 교체했다.
- 이유: 처음 쓴 코드가 실제 소스와 달랐다. 기억에 의존해 지어낸 것이었다.
  실제 `read_window`는 `length`와 `circular` 인자를 받지 않는다.
  `_oriented`라는 함수도 존재하지 않는다.
  실제로는 방향에 따라 `Region`을 직접 만들어 돌려준다.
  발표 중 매니저가 코드를 짚었을 때 존재하지 않는 코드를 설명하게 될 뻔했다.
- 검토한 대안: 부록 3을 빼는 것도 고려했다. 실제 코드가 17줄로 짧고 폭도 맞아 그대로 실을 수 있어 유지했다.
- 보수적 선택 여부: 예. 앞으로 슬라이드에 넣는 모든 코드는 원본 파일을 직접 읽어 복사한다.
- 계획과의 이탈: 없음. 설계 문서가 지정한 `core.py` 216~232 범위를 그대로 따른다.
- 사용자 확인 필요: 아니오

## 2026-07-27 — 부록 3의 글꼴 크기를 설계 문서 값에서 변경

- 결정: 부록 3 코드의 글꼴을 17.9픽셀(0.426em)로 정했다.
- 이유: 실제 코드의 가장 넓은 줄이 86칸이었다. `dead_zone_region`의 선언 줄이다.
  설계 문서는 부록 코드의 크기를 정해 두지 않았다.
  86칸이 들어가려면 17.9픽셀이 상한이다.
  17줄이므로 세로는 20줄까지 들어가 여유가 있다.
- 검토한 대안: 없음. 폭에서 상한이 나오므로 선택의 여지가 없다.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음. 설계 문서가 부록 코드 크기를 비워 둔 자리를 채운 것이다.
- 사용자 확인 필요: 아니오

## 2026-07-27 — 설계 문서의 세로 계산이 부족했음을 브라우저 실측으로 발견

- 결정: 슬라이드 4장의 글꼴을 설계 문서 값보다 더 줄였다.
- 이유: 설계 문서는 코드 상자 하나가 448픽셀 안에 들어가는지만 계산했다.
  실제 슬라이드에는 제목과 설명 문구도 함께 들어간다.
  브라우저로 열어 재 보니 슬라이드 네 장이 700픽셀을 넘었다.
  넘친 부분은 화면 아래에서 잘린다.
  넘긴 장은 코드 두 장과 파일 역할표와 약점 첫 장이다.
  각각 774, 750, 714, 735픽셀이었다.
- 검토한 대안: 설명 문구를 발표자 노트로 내리는 방법도 있었다.
  그 문구들이 매니저가 화면에서 봐야 할 내용이라 남기고 글꼴만 줄였다.
- 보수적 선택 여부: 예. 글자를 줄이는 쪽이 내용을 빼는 쪽보다 되돌리기 쉽다.
- 계획과의 이탈: 있음. 설계 문서가 정한 글꼴 크기 두 개를 더 줄였다.
  코드 두 장은 14.5픽셀에서 더 내려갔다.
  다만 폭 제약은 그대로 만족한다. 줄이면 폭은 더 여유로워진다.
- 사용자 확인 필요: 아니오

## 2026-07-27 — 한글 폭 문제가 예측보다 심각해 런타임 보정을 넣음

- 결정: 터미널 원문 블록의 한글 폭을 화면에서 재서 모자란 만큼 자간으로 채우는 코드를 넣었다.
  코드는 30줄 정도이고 발표자료 파일 안에 들어 있다.
- 이유: 설계 문서는 "정렬이 어긋나도 덮임 그림의 한 줄만 밀린다"고 판단했다.
  브라우저로 열어 보니 그 판단이 틀렸다.
  이 컴퓨터에서는 한글이 라틴 문자의 1.66배로 그려졌다.
  2배가 아니라서 덮임 그림의 합계 줄이 8.7픽셀 밀렸다.
  추천 표는 한글이 여러 열에 있어 표 전체가 어긋났다.
  등폭 한글 글꼴 후보를 열두 개 시험했는데 이 환경에는 하나도 없었다.
  그래서 글꼴을 지정하는 방법으로는 고칠 수 없었다.
- 검토한 대안: 첫째, 한글 등폭 글꼴 파일을 저장소에 넣는 방법이 있다.
  파일이 무겁고 설계 문서가 범위 밖으로 정한 항목이라 버렸다.
  둘째, 합계 레이블을 영문으로 바꾸는 방법이 있다.
  덮임 그림은 고쳐지지만 추천 표는 한글이 너무 많아 못 고친다.
  셋째, 원문을 표로 다시 조판하는 방법이 있다.
  그러면 실제 실행 화면이 아니게 되어 버렸다.
- 보수적 선택 여부: 예. 이미 2배로 그려지는 글꼴에서는 아무것도 건드리지 않고 넘어간다.
  실제 폭을 재서 차이가 0.05픽셀 미만이면 보정하지 않는다.
- 계획과의 이탈: 있음. 설계 문서는 이 문제를 "발표 전 점검 항목"으로 미뤘다.
  실제로는 지금 이 자리에서 이미 깨져 있어 미룰 수 없었다.
  설계 문서의 "한 줄만 밀린다"는 판단도 사실과 달랐다.
- 사용자 확인 필요: 아니오. 보정 후 실측으로 정렬을 확인했다.
  덮임 그림의 네 지점이 모두 같은 위치가 되었다.
  추천 표의 보관 위치 열 다섯 행과 녹는점 열 세 행도 각각 같은 위치가 되었다.

## 2026-07-27 — 코드 슬라이드에서 영어 설명글 7줄을 뺌

- 결정: 결합 판정 코드에서 원본의 영어 설명글 일곱 줄을 빼고 대신 뺐다는 표시를 한 줄 넣었다.
- 이유: 그 슬라이드가 세로 737픽셀로 화면 상한 700픽셀을 넘었다.
  글꼴을 더 줄이면 읽을 수 없을 만큼 작아졌다.
  영어 설명글의 내용은 이미 슬라이드 아래에 한국어로 적혀 있었다.
  매니저가 영어 설명글을 읽을 이유가 없다.
- 검토한 대안: 한국어 캡션을 발표자 노트로 내리는 방법도 있었다.
  그 캡션이 이 슬라이드의 요점이라 화면에 남겨야 했다.
- 보수적 선택 여부: 예. 코드를 고치지 않고 화면에서만 뺐다. 뺐다는 사실도 화면에 적었다.
- 계획과의 이탈: 있음. 설계 문서는 이 함수를 25줄 통째로 실으라고 했다.
  실제로는 19줄만 싣는다.
- 사용자 확인 필요: 아니오

## 2026-07-27 — 약점 슬라이드 두 장을 큰 제목 형식에서 일반 형식으로 바꿈

- 결정: 5부의 두 장에서 큰 제목 스타일을 벗기고 일반 슬라이드로 만들었다.
- 이유: 큰 제목과 부제가 세로 공간을 많이 차지해 슬라이드가 735픽셀이 되었다.
  화면 상한 700픽셀을 넘어 아래가 잘렸다.
  항목 글자를 줄이는 것만으로는 부족했다.
- 검토한 대안: 약점 다섯 가지를 세 장으로 나누는 방법도 있었다.
  본론 장수가 24장에서 25장이 되어 상한에 붙는다.
  제목 스타일만 바꾸면 되므로 그쪽을 택했다.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 있음. 설계 문서는 이 두 장의 형식을 정하지 않았으나, 다른 구분 슬라이드와 형식이 달라졌다.
- 사용자 확인 필요: 아니오

## 2026-07-27 — 검토에서 밝은 모드 대비 결함을 발견하고 수정

- 결정: 표와 글을 담는 데 쓰던 상자 꾸밈(`.figure`) 네 곳을 모두 없앴다.
- 이유: 그 상자는 다이어그램과 그래프 전용이다.
  밝은 화면 모드에서 배경만 어둡게 고정되고 글자색은 밝은 모드 값을 그대로 쓴다.
  그래서 어두운 상자에 어두운 글자가 되어 대비가 1.04 대 1이 되었다.
  사실상 읽을 수 없는 상태였다.
  하필 이 모드는 조명이 밝은 강의실에서 쓰라고 안내한 기능이다.
  필요한 순간에 정확히 깨지는 결함이었다.
  견본 발표자료는 이 상자에 색을 가진 그림만 넣었기 때문에 문제가 드러난 적이 없었다.
- 검토한 대안: 밝은 모드에서만 글자색을 밝게 바꾸는 방법도 있었다.
  공용 스타일 파일을 고쳐야 해서 견본 발표자료에도 영향을 준다.
  이번 작업은 발표자료 추가이므로 공용 파일은 건드리지 않았다.
- 보수적 선택 여부: 예. 새 발표자료 안에서만 고쳤다.
- 계획과의 이탈: 없음. 설계 문서가 이 꾸밈을 쓰라고 정한 적이 없다.
- 사용자 확인 필요: 아니오. 수정 후 28장 전부의 대비를 실측해 4.5 대 1 이상을 확인했다.

## 2026-07-27 — 글자 폭 보정 코드를 실측 기반으로 재작성

- 결정: 문자 종류를 정규식으로 추측하던 방식을 버렸다.
  이제 글자마다 실제 폭을 재서 목표 칸에 모자란 만큼을 채운다.
- 이유: 처음 방식은 한글과 한자만 보정했다.
  긴 줄표와 화살표는 글꼴에 따라 한 칸도 두 칸도 된다.
  이 컴퓨터에서는 우연히 한 칸이라 문제가 없었다.
  한글 글꼴이 깔린 컴퓨터에서는 두 칸이 되어 어긋난다.
  설계 문서가 남긴 열린 질문이 바로 이 긴 줄표였는데 첫 방식은 그 질문을 닫지 못했다.
- 함께 고친 것: 글꼴 정보를 못 읽는 브라우저에서 본문 글꼴로 잘못 재던 문제를 막았다.
  잘못 재면 보정이 안 되는 게 아니라 원본보다 더 크게 망가진다.
  보정 폭이 한 칸을 넘으면 측정이 틀린 것으로 보고 건드리지 않는다.
  보정이 실패해도 슬라이드는 넘어가도록 예외를 따로 감쌌다.
  발표자 화면에서는 다시 재지 않고 건너뛴다.
- 검토한 대안: 정규식에 긴 줄표와 화살표를 더하는 방법도 있었다.
  다음에 다른 문자가 들어오면 같은 문제가 또 생겨서 버렸다.
- 보수적 선택 여부: 예. 이미 맞은 글자는 손대지 않는다.
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오. 수정 후 정렬을 다시 실측해 그대로 맞는 것을 확인했다.

## 2026-07-27 — 발표자 노트의 부록 위치 번호를 화면 번호로 정정

- 결정: 마지막 장 발표자 노트의 부록 위치를 11번, 19번, 24번으로 고쳤다.
- 이유: 원래 11번, 18번, 21번으로 적혀 있었다.
  그것은 본론만 센 번호였다.
  화면 오른쪽 아래에 나오는 번호는 부록까지 포함해 센다.
  발표 중 질문을 받고 노트를 보는 순간 엉뚱한 곳으로 가게 된다.
- 검토한 대안: 없음. 실측해서 맞는 번호를 넣었다.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오

## 2026-07-27 — 자동 검사 사각지대를 범위 밖으로 유지 (확인 필요)

- 결정: 새 발표자료를 자동 링크 검사 목록에 넣지 않고 그대로 두었다.
- 이유: 요구사항 문서와 설계 문서가 이 항목을 범위 밖으로 정했다.
  구현 도중에 범위를 넓히지 않는다.
- 그런데 새로 알게 된 사실이 있다.
  이 발표자료에는 글자 폭을 보정하는 코드가 60줄 들어 있다.
  자동 검사는 확장자가 `.js`인 파일만 문법을 본다.
  이 코드는 발표자료 파일 안에 있어 문법이 틀려도 검사를 통과한다.
  발표자료가 참조하는 파일 경로도 검사되지 않는다.
  범위를 정할 때는 발표자료에 코드가 들어갈 줄 몰랐다.
- 검토한 대안: 검사 목록에 한 줄을 더하는 방법이 있다.
  한 줄이면 되고 이득이 크지만 범위를 넘는 결정이라 미뤘다.
- 보수적 선택 여부: 예. 범위를 넓히지 않고 그대로 두었다. 대신 이번에는 손으로 확인했다.
  참조 경로 11개가 모두 실재함을 확인했다.
  발표자료 안의 코드도 따로 뽑아 문법 검사를 통과시켰다.
- 계획과의 이탈: 없음
- 사용자 확인 필요: **예.** 자동 검사 목록에 새 발표자료를 넣을지 물어야 한다.

## 2026-07-27 — 사용자 확인 결과, 자동 검사에 발표자료를 포함하기로 함

- 결정: 자동 검사 목록에 새 발표자료를 넣었다. 검사 항목도 하나 더 만들었다.
  첫째, 검사할 페이지 목록에 새 발표자료를 더했다.
  둘째, 페이지 안에 직접 쓴 스크립트의 문법을 검사하는 항목을 새로 넣었다.
- 이유: 사용자가 포함하라고 답했다.
  목록에만 더하면 참조 경로만 검사된다.
  발표자료 안에 직접 쓴 코드는 여전히 검사되지 않는다.
  워크플로가 문법을 보는 대상은 확장자가 `.js`인 파일뿐이기 때문이다.
  그래서 검사 파일 자체에 항목을 하나 더 만들었다.
- 검토한 대안: 워크플로에 단계를 더해 코드를 뽑아 검사하는 방법도 있었다.
  검사 파일이 이미 각 페이지를 읽고 있어서 그쪽에 넣는 편이 짧았다.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 있음. 요구사항과 설계 문서가 범위 밖으로 정했던 항목이다.
  사용자가 포함을 지시해 범위에 들어왔다.
- 사용자 확인 필요: 아니오. 해소되었다.
- 검증: 일부러 문법을 깨뜨려 보니 검사가 잡아냈다.
  일부러 경로를 깨뜨려 보니 그것도 잡아냈다.
  되돌린 뒤 다시 통과하는 것도 확인했다.

## 2026-07-27 — 부록 4장의 부착 위치 확정

- 결정: 부록을 관련 본문 슬라이드 아래에 붙였다.
  부록 3(판독 설정 코드)은 11번 슬라이드 아래에 붙였다.
  부록 1(버린 방법)과 부록 2(성능)는 18번 슬라이드 아래에 붙였다.
  부록 4(안 만든 기능)는 21번 슬라이드 아래에 붙였다.
- 이유: 설계 문서는 부록을 세로로 붙이라고만 했고 어느 슬라이드인지는 정하지 않았다.
  질문이 나올 자리 바로 아래에 두어야 발표 중에 즉시 꺼낼 수 있다.
- 검토한 대안: 부록 4장을 맨 뒤에 몰아 두는 방법도 있었다.
  그러면 질문이 나올 때마다 맨 뒤까지 이동해야 해서 버렸다.
- 보수적 선택 여부: 아니오
- 계획과의 이탈: 없음
- 사용자 확인 필요: 아니오
